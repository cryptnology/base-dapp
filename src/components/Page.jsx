import * as s from '../styles/globalStyles';

const Page = () => {
  return (
    <s.Container flex={1} ai={'center'} jc={'center'}>
      <h1>Page</h1>
    </s.Container>
  );
};

export default Page;
